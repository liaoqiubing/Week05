按照老师的教程，我尝试在llama官网申请下载llama7B的权重，第一次失败了，后面调整了申请信息成功下载。

在代码里尝试使用官方下载的权重去训练，但是报错了，根据报错信息查询资料提示拿到的是原始 Meta 权重（只有 consolidated.00.pth、params.json、checklist.chk）。
这种格式不能被 Transformers 直接加载，必须先“转换为 Hugging Face(HF) 目录结构”。

按照资料尝试了几次没有成功，后面就按照教程提供的model进行训练。

按照完整训练3个epoch，发现需要36个小时才能训练完，由于是共享账号，所以只训练1个epoch，同时把maxstep属性移除，这样就能完整训练一个epoch。

训练过程loss比较稳定，推理的也正常，后续有空可尝试使用中文数据集进行训练，尤其是教程中提及的“弱智吧”数据集，感觉蛮有意思的！
